<footer class="bg-surface-50 hotwire-native:hidden">
  <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-4">
    <div class="flex flex-col items-center space-y-4">
      <div class="flex items-center space-x-4">
        <%= render LanguageSwitcherComponent.new(current_locale: I18n.locale) %>
        <%= link_to t("privacy"), pages_privacy_path, class: "text-sm text-gray-600 hover:text-gray-900" %>
      </div>
      <p class="text-center text-gray-600 text-sm">&copy; <%= current_year %> BentoBook. <%= t("all_rights_reserved") %></p>
    </div>
  </div>
</footer>