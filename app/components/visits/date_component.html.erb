<%= turbo_frame_tag frame_id do %>
  <%= link_to edit_visit_date_path(visit_id: visit.id, locale: current_locale),
      class: link_classes,
      data: { turbo_frame: helpers.hotwire_native_app? ? "_top" : frame_id } do %>
    <%= visit.date.strftime("%B %d, %Y") %>
  <% end %>
<% end %>