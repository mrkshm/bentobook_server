<div class="container mx-auto p-4 safe-area-inset">
  <h1 class="text-3xl font-bold text-primary mb-8 mt-12">Welcome to BentoBook</h1>
  
  <% if user_signed_in? %>
    <%= link_to "View Restaurants", restaurants_path, class: "btn btn-primary w-full" %>
  <% else %>
    <%= link_to "Sign In", new_user_session_path, class: "btn btn-primary w-full" %>
  <% end %>
</div>