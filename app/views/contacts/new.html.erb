<h1><%= t('contacts.new.title') %></h1>

<%= render 'form', contact: @contact, submit_text: t('contacts.new.create_contact') %>

<%= link_to t('back_to_contacts'), contacts_path, class: 'btn btn-secondary' %>
