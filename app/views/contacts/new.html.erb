<%= render 'shared/header', title: t('contacts.new.title') %>

<%= render 'form', contact: @contact, submit_text: t('contacts.new.create_contact') %>

<div class="mt-6 text-center">
  <%= link_to t('back_to_contacts'), contacts_path, 
      class: "inline-flex items-center px-4 py-2 rounded-md text-sm font-medium text-surface-700 bg-surface-100 hover:bg-surface-200 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-surface-500" %>
</div>
