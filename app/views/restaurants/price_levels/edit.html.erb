<%= turbo_frame_tag "edit_restaurant_price_level" do %>
<div class="bg-surface-50 p-4">
    <%= form_with model: @restaurant, 
                  url: restaurant_price_level_path(restaurant_id: @restaurant.id, locale: current_locale),
                  method: :patch,
                  class: "bg-white rounded-lg shadow p-4" do |f| %>
      <div class="flex justify-center space-x-2">
        <% 4.times do |i| %> 
          <%= f.button type: "submit",
                      name: "restaurant[price_level]",
                      value: i + 1,
                      class: "focus:outline-none" do %>
            <%= currency_icon(size: :lg, class: "#{i < @restaurant.price_level.to_i ? 'text-primary-500' : 'text-surface-300'}") %>
          <% end %>
        <% end %>
      </div>
    <% end %>
  </div>
<%end%>
