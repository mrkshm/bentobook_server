<%# Remove the modal by replacing it with nothing %>
<%= turbo_stream.update "modal", "" %>

<%# Update the rating display %>
<%= turbo_stream.replace dom_id(@restaurant, :rating), partial: "restaurants/ratings/display", locals: { restaurant: @restaurant.reload } %>